# E4 机器人足球队统一开发规范

## 1. 目的与适用范围

本规范适用于 E4 机器人足球项目的所有成员。目标是共同完成一份能够独立解释、独立维护、符合课程学术诚信要求的原创比赛程序。

GitHub 仓库只维护以下文件：

```text
src/client.cpp
run.sh
.gitignore
.gitattributes
开发规范.md
```

每位成员本地保留完整 `team1` 编译环境，但不得把服务器源码、日志、缓存、编译产物或个人备份提交到仓库。

## 2. 学术诚信红线

以下行为一律禁止：

1. 复制 UVA、其他大学、其他小组、往届学生或网络仓库中的球队策略代码。
2. 接收其他队伍的完整 `client.cpp` 或核心函数后改名、调序、删注释再提交。
3. 要求 AI 模仿某个公开项目、改写已有代码、隐藏代码来源或规避查重。
4. 通过变量重命名、无意义封装、格式扰动、插入冗余代码等方式伪装复制内容。
5. 删除原作者声明、许可证或来源注释，将第三方代码冒充为本组原创代码。
6. 提交自己无法逐行解释、无法说明设计原因或无法独立调试的代码。
7. 将其他队伍的未公开策略、比赛代码或平台漏洞带入本项目。

发现来源不清或疑似复制的代码时，停止合并，由提交者说明设计过程和来源；无法说明时直接移除并重新独立实现。

## 3. 可以使用的参考资料

允许用于学习和查阅的资料包括：

- 教师提供的实验要求、课件和支持文档。
- RoboCup Soccer Server 官方协议和命令说明。
- C/C++、Bash、Git 等工具的官方文档。
- 通用算法、数学公式和数据结构教材。
- 本组成员自己在前序实验中独立完成的代码。

查阅参考资料时，只学习接口、规则和算法思想，不复制球队实现。若确实需要使用第三方库或少量第三方代码，必须先确认课程允许，并在代码评审中记录来源、许可证和使用理由。

## 4. AI 辅助开发规范

AI 可以作为解释、评审和调试工具，但不能替代成员完成课程设计。使用 AI 时必须遵守以下流程。

### 4.1 提问前

1. 先用自己的语言写清问题、输入、输出和限制条件。
2. 先提出自己的基本方案或伪代码，再让 AI 检查遗漏。
3. 不向 AI 提供其他队伍、UVA 或网络仓库的代码并要求改写。

### 4.2 AI 输出后

1. 不直接复制整段输出并提交。
2. 逐行检查使用的协议命令、状态变量、边界条件和错误处理。
3. 确认每个函数都服务于本组明确提出的战术，而不是来源不明的通用球队框架。
4. 删除未经验证、项目不需要或成员无法解释的部分。
5. 在本地完成编译、单功能测试和完整比赛测试。
6. 由另一名成员进行代码评审，重点询问“为什么这样设计”。

对 AI 建议的采纳必须基于理解和验证。成员可以根据本组设计重新实现思路，但不得进行仅为躲避检测的表面改写。

### 4.3 使用记录

小组在共享文档中记录重要 AI 辅助过程，至少包括：

- 日期和负责成员。
- 当时要解决的问题。
- 使用的工具或模型。
- AI 给出的主要建议。
- 实际采纳、修改或拒绝了哪些内容。
- 对应测试结果。

如果教师或课程要求披露 AI 使用情况，所有成员必须如实提交，不得隐瞒。

## 5. 原创设计要求

每项策略开发前应先形成简短设计说明，至少回答：

1. 该球员承担什么角色？
2. 使用哪些 `(see ...)`、`(hear ...)` 或身体状态信息？
3. 状态如何转换，退出条件是什么？
4. 左右场如何保持行为对称？
5. 看不到球、队友或球门时如何恢复？
6. 可能与哪些现有策略冲突？
7. 用什么场景验证实现有效？

代码应由这些设计自然推导出来。评审时，提交者必须能够脱离 AI 对话和参考页面说明完整决策流程。

## 6. 代码风格

1. 使用清晰、稳定、能够表达足球含义的英文变量名和函数名。
2. 一个函数只负责一个明确行为，避免把所有策略堆在单个巨大条件分支中。
3. 状态值、距离阈值和角度阈值应有明确含义；重要常量集中定义。
4. 只在决策原因不明显时添加简短注释，注释必须由作者用自己的语言编写。
5. 不加入无意义代码、死代码、混淆逻辑或为了制造差异而增加的包装层。
6. 不使用个人绝对路径、账号、令牌、IP 地址或只在个人环境中存在的文件。
7. `run.sh` 必须使用 LF 换行并保留可执行权限。
8. 正式比赛固定启动 5 名球员，队名、左右场和服务器参数必须符合平台要求。

## 7. Git 协作要求

每次开始开发前更新 `main` 并创建功能分支：

```bash
git switch main
git pull --ff-only origin main
git switch -c feature/功能名称
```

只提交本次实际修改的协作文件：

```bash
git add src/client.cpp run.sh 开发规范.md
git diff --cached
git commit -m "feat: describe the strategy change"
git push -u origin feature/功能名称
```

禁止把完整本地 `team1`、比赛录像、日志和编译结果强制加入 Git。禁止直接向 `main` 强制推送。

每个功能分支应保持主题单一。提交信息要说明实际行为变化，不使用“update”“改了一下”等无法追溯的描述。

## 8. 代码评审清单

Pull Request 合并前至少由一名非提交者检查：

- [ ] 提交者能说明需求、设计和每个关键分支。
- [ ] 没有复制或改写其他球队、UVA 或网络项目的实现。
- [ ] AI 参与部分已逐行理解，并记录在小组共享文档中。
- [ ] 没有为了规避检测而进行变量改名、结构扰动或代码混淆。
- [ ] 只修改了本次任务需要的文件和逻辑。
- [ ] 左右场方向、球门选择和队名逻辑正确。
- [ ] 看不到目标、通信丢失和动作失败时能够恢复。
- [ ] 5 名球员的职责不会明显冲突或全部追球。
- [ ] 本地编译成功，相关场景完成测试。
- [ ] `git diff` 中没有日志、缓存、密钥、绝对路径和个人信息。

不满足任何一项时不得合并。

## 9. 测试要求

每次策略修改至少完成三层验证：

1. 静态检查：检查参数解析、数组范围、空指针、消息解析和未初始化变量。
2. 单场景测试：只观察本次修改的角色和行为是否符合设计。
3. 完整 5v5 测试：检查角色冲突、攻守转换、左右场和长时间运行稳定性。

测试记录应写明 commit ID、测试场景、预期结果、实际结果和发现的问题。比赛日志保存在本地或共享测试目录，不进入 GitHub 仓库。

## 10. 最终提交审计

正式比赛和课程提交前，由全组共同完成：

1. 从远端 `main` 获取最新代码并记录 commit ID。
2. 在一台未使用个人缓存的环境中重新编译。
3. 检查 `client.cpp` 和 `run.sh` 是否是平台要求的最终文件。
4. 每位成员口头说明自己负责的模块和关键决策。
5. 检查所有代码来源和 AI 使用记录是否完整、真实。
6. 删除来源不明、无法解释、未经测试或课程不允许的内容。
7. 完成至少一次左场和一次右场的 5v5 比赛测试。

本组以“能够证明设计过程、能够解释代码、能够复现实验结果”为合格标准。任何查重或教师审查都不应通过伪装规避，而应通过真正的原创实现和透明记录来经受检查。
